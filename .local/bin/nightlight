#!/bin/sh

CHOICE=$(printf "on\noff\nextreme" | dmenu -p "Turn nightlight on/off")
case $CHOICE in
    on)
        redshift -PO 3200 ;;
    off)
        redshift -PO 6500 ;;
    extreme)
        redshift -PO 1300 ;;
    *)
        exit ;;
esac
