#!/bin/bash

# auto-detect monitors
external_monitor=$(xrandr --query | grep -w 'DP1')
if [[ $external_monitor = *connected* ]]; then
        xrandr --output eDP1 --mode 1366x768 --pos 0x312 --output DP1 --primary --mode 1920x1080 --pos 1366x0
fi
