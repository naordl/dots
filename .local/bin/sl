#!/bin/sh

# Compile suckless programs
# First we check if the header file exist in the current directory
# If it does, we compile the program using `make`
[ -e config.h ] && sudo make clean install && exit 0 || \
[ -e blocks.h ] && sudo make clean install && exit 0 || \

# If we can't compile, print an error message
printf "ERROR: Failed compiling suckless program.
Can't find header files. Is this the correct directory...?\n"
