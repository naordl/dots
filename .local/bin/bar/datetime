#!/bin/sh

# Mouse actions
case $BLOCK_BUTTON in
    1) setsid -f $TERMINAL -e calcurse ;;
esac

# Get current time
TIME=$(date "+%I:%M%P")

# Get the current date
DATE=$(date "+%d/%m/%Y")

# Print result
printf " $TIME  $DATE\n"
