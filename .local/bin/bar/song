#!/bin/sh

# dependencies: mpc, mpd, playerctl

case $BLOCK_BUTTON in
    1) mpc toggle ;;
    4) mpc next ;;
esac

STATE=$(mpc -v | grep -o playing)

if [ "$STATE" != "playing" ]; then
    printf ""
fi

if [ "$STATE" = playing ]; then
    SONG=$(mpc -v | awk NR==1)
    printf " $SONG\n"
fi
