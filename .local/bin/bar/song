#!/bin/sh

# dependencies: mpc, mpd

STATE=$(mpc -v | grep -o playing)

if [ "$STATE" != "playing" ]; then
    printf ""
fi

if [ "$STATE" = playing ]; then
    SONG=$(mpc -v | awk NR==1)
    printf " $SONG\n"
fi
